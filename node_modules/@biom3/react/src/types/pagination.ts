export type PaginationDiscriminatedUnion<T> =
  | (T & {
      currentPage: number;
      onPageChange: (value: number) => void;
    })
  | (T & {
      defaultPage?: number;
      onPageChange?: (value: number) => void;
      currentPage?: never;
    });
